{
  "name": "nvlink",
  "version": "1.0.0",
  "private": true,
  "description": "Static NVLink explainer site for nvlink.lol.",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test",
    "test:browser": "playwright test",
    "brand": "python3 scripts/generate_brand_assets.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}

